Real checker lesson asset provenance
====================================

Source fixture:
`fixtures/real-analysis/checker-sage-gnu-disagreement-001/`

Identity:

- position_id: checker-sage-gnu-disagreement-001
- state_hash: e0b88727ba5d0cc170a207d4a9f2d44039f7a30d94ab05b6f0a30b2118d35070
- analysis_id: analysis-sha256-69690b1d0bb2475087e8d11b31303f17a12a4e2e4ddfb8e0d4486462c5afd14d

The source JSON is an exact tracked copy of the Codex 2 synergy artifact. The
website projection is generated by `scripts/project_real_checker_fixture.py`.
It reads `analyzer-view.json` directly and joins probabilities from the
authoritative `analysis.json`; it does not rewrite either source document.

The SVGs were generated with backgammonboard commit
`b2749d411035243a80ad209d4da0cb9b0d7bae29` by
`scripts/render_real_checker_assets.R`. The script renders the starting state,
applies each selected move at build time, and refuses to save a candidate when
the resulting checker arrangement differs from `analyzer-view.json`.

The browser only swaps the pre-rendered SVG path and metrics supplied in the
website projection. It does not parse move notation or apply checker moves.
