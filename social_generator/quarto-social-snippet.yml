# Merge these entries into the existing _quarto.yml.
# Keep your existing R content/QMD/manifest generation commands before this one.

project:
  pre-render:
    - python ../social_generator/scripts/social/run_social_pipeline.py

website:
  image: /assets/social/generated/social-default.png
  open-graph: true
  twitter-card:
    card-style: summary_large_image
