# Development SOP pointer

For local preview, use `bash scripts/preview-site.sh [PORT]` after a full
render exists. For build and release behavior, follow the existing
`scripts/bs-build-and-publish.sh` contract and the root testing SOP. This is a
layout reservation, not a replacement workflow.
